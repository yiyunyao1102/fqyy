# Art Bible

- `accepted_examples/`: visual references that match the target xiuxian side-view gameplay readability.
- `rejected_examples/`: references that drift into low-readability detail, wrong perspective, or the wrong genre feel.

When adding references, note why each image is accepted or rejected so future art decisions stay consistent.

# Sprite Folders

- `source/`: editable Aseprite files
- `raw_ai/`: untouched AI draft outputs
- `export/`: production PNG sprite sheets imported by the game

Each sprite category follows the same workflow described in `docs/SPRITE_WORKFLOW.md`.
